Convert this entire web application into a production-ready mobile app (APK) and finalize the admin panel for deployment.

## 🎯 GOALS

1. App must work as a full mobile app (APK) installable on Android without Play Store.
2. App must also be ready for Google Play Store publishing.
3. Admin panel must be fully production-ready and connected in real-time with Firebase.
4. Both app and admin panel must use the SAME backend and show live synchronized data.

---

## 🔥 BACKEND (CRITICAL)

* Use Firebase Firestore as the ONLY database.
* Ensure all modules are fully integrated and persistent:

Collections:

* users
* appointments
* workers
* categories
* cities
* reviews
* advertisements
* messages

Every module must:

* Create (addDoc)
* Read (getDocs / real-time listener)
* Update (updateDoc)
* Delete (deleteDoc)

NO fake/local state data allowed.

---

## 📱 MOBILE APP REQUIREMENTS (APK READY)

1. Convert web app into APK using proper mobile wrapper or framework (Capacitor / React Native / Flutter WebView ONLY if needed properly).
2. App must:

   * Install and run without Play Store
   * Work offline gracefully (basic UI caching if possible)
   * Sync automatically when internet is available
3. UI must be mobile responsive and optimized for Android screens.
4. Firebase authentication must work properly on mobile.

---

## 🧑‍💼 ADMIN PANEL REQUIREMENTS

1. Admin panel must:

   * Show live Firestore data (real-time updates preferred)
   * Allow full CRUD operations for all collections
   * Reflect changes instantly in user app
2. Add admin authentication (secure login system)
3. Prevent unauthorized access (role-based access: admin only)

---

## 🔄 SYNC REQUIREMENTS

* Any change in admin panel must instantly reflect in mobile app.
* Any booking or user action in app must instantly appear in admin panel.
* Use Firestore real-time listeners (onSnapshot) where possible.

---

## ❌ REMOVE THESE ISSUES

* No fake success messages without Firestore confirmation
* No demo/hardcoded data
* No local-only state storage for important data
* No missing collection writes

---

## 🏁 FINAL OUTPUT

Provide:

1. Fully working mobile APK build setup
2. Production-ready admin panel
3. Fully connected Firebase backend
4. Deployment instructions for Play Store (AAB + signing)
5. Clean, error-free codebase

Make sure everything is production-ready and scalable.